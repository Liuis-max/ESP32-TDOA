# 三麦克风 TDOA 声源 2D 定位算法

> 基于 GCC-PHAT + Fang 算法 | 16kHz 采样率 | 声速 340m/s | 麦克风间距 17cm

---

## 1. 系统架构总览

```
CSV数据输入 (L/R/C三通道)
        │
        ▼
┌──────────────────┐
│   逐帧预处理       │  DC去除 + 汉宁窗 + 300-3400Hz带通滤波
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  GCC-PHAT互相关   │  计算 lag12 (MIC1-MIC2) 和 lag13 (MIC1-MIC3)
└──────┬───────────┘
       │
       ├──────────────────────┐
       ▼                      ▼
┌──────────────┐    ┌──────────────────┐
│  角度估计     │    │  TDOA距离差      │
│  θ12, θ13   │    │  d12 = Δt12·c   │
│              │    │  d13 = Δt13·c   │
└──────┬───────┘    └────────┬─────────┘
       │                     │
       ▼                     ▼
┌──────────────┐    ┌──────────────────┐
│ Theta三角定位 │    │  Fang算法精确解   │  (置信度阈值过滤)
│   (远场近似)  │    │
└──────┬───────┘    └────────┬─────────┘
       │                     │
       └──────────┬──────────┘
                  ▼
         声源坐标 (x, y)
```

### 数据流说明

| 处理阶段 | 输入 | 输出 |
|----------|------|------|
| CSV 加载 | mic_data_*.csv 文件 | (L, R, C) 三通道帧列表，每帧 512 采样点 |
| 预处理 | 原始 ADC 采样值 | 去直流、加窗、带通滤波后的信号 |
| 时延估计 | 预处理后的信号对 (L,R) 和 (L,C) | 时延 lag (样本数)、置信度 |
| 距离转换 | lag12, lag13 | d12, d13 (米) |
| 定位解算 | d12, d13 | 声源坐标 (x, y) |

---

## 2. 麦克风阵列布局

**阵列几何 (平面坐标系)**

```
        Y (m)
        ^
        |
        |
MIC1 ●──|────────● MIC2
(0,0)   |       (D, 0)    D = 0.17m
        |
        |
        ● MIC3
      (0, -D)
```

| 麦克风 | 坐标 | 对应通道 | 角色 |
|--------|------|----------|------|
| MIC1 | (0, 0) | L 通道 (GND) | 参考原点 |
| MIC2 | (D, 0) | R 通道 (3.3V) | X 轴基线 |
| MIC3 | (0, -D) | C 通道 | Y 轴基线 |

- **布局类型：** 正交 L 型三麦克风阵列
- **X 轴基线：** MIC1 ↔ MIC2，间距 17cm
- **Y 轴基线：** MIC1 ↔ MIC3，间距 17cm
- **优势：** 两条正交基线直接解耦 X/Y 方向的 TDOA

---

## 3. 信号预处理

### 三步流水线

| 步骤 | 方法 | 目的 |
|------|------|------|
| 1. DC 去直流 | `s = s - mean(s)` | 消除 ADC 直流偏置 |
| 2. 汉宁窗 | `s × Hanning(n)` | 抑制频谱泄漏，减少 FFT 旁瓣 |
| 3. 频域带通滤波 | FFT → 保留 300-3400Hz → IFFT | 提取语音频段，抑制带外噪声 |

### 关键参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 采样率 | 16000 Hz | 语音信号奈奎斯特频率以上 |
| 帧长度 | 512 采样点 | 约 32ms 帧长 |
| 带通范围 | 300 - 3400 Hz | 语音基频 + 谐波范围 |

```
原始信号 ──► [-mean] ──► [×Hanning] ──► [FFT] ──► [300-3400Hz mask] ──► [IFFT] ──► 输出
```

---

## 4. GCC-PHAT 时延估计

### 广义互相关-相位变换 (Generalized Cross-Correlation with Phase Transform)

**算法流程：**

```
信号1 ──► FFT ──► S1(f)
                        │
           R(f) = S1(f)·S2*(f)     ← 互功率谱
                        │
      R_PHAT(f) = ─────────────    ← 相位加权 (仅保留相位信息)
                    |R(f)| + ε
                        │
                        ▼  IFFT
               互相关函数 gcc(t)
                        │
                  argmax |gcc(t)|  → 时延 lag
                        │
                  二次插值 → 亚采样点精度时延
```

### 核心公式

互功率谱：  
**R(f) = S1(f) · S2\*(f)**

PHAT 加权后：  
**R_PHAT(f) = R(f) / (|R(f)| + ε)**

时延：  
**lag = argmax |IFFT(R_PHAT)|**

### 二次插值 (亚采样点精度)

```
         (y_left - y_right)
sub = ──────────────────────────
       2 × (y_left - 2×y_peak + y_right)

lag_fine = lag + sub
```

### 优势

- **白化处理：** 幅度归一化，仅保留相位差信息
- **抗混响：** 对多径传播不敏感
- **鲁棒性：** 在低信噪比下仍能可靠估计时延

---

## 5. TDOA → 角度计算

### 远场平面波假设

对于基线间距 D，时延 Δt：

```
sin(θ) = Δt · c / D = d / D

θ = arcsin( clip(d/D, -1, 1) )
```

| 角度 | 对应基线 | 含义 |
|------|----------|------|
| θ12 | MIC1 → MIC2 (X轴) | 声源在 X 方向的到达角 |
| θ13 | MIC1 → MIC3 (Y轴) | 声源在 Y 方向的到达角 |

### 物理约束

```
|d12| ≤ D  （ΔT12 不超过几何最大延迟）
|d13| ≤ D  （ΔT13 不超过几何最大延迟）
```

最大几何延迟 = D / c ≈ 0.5ms @ 17cm

---

## 6. Fang 算法 (精确定位)

### 问题建模

已知：
- r2 - r1 = d12（MIC1-MIC2 距离差）
- r3 - r1 = d13（MIC1-MIC3 距离差）
- MIC1 = (0, 0), MIC2 = (D, 0), MIC3 = (0, -D)

求解 (x, y)。

### 推导过程

**第一步：** 由双曲线方程 r2² - r1² 和 r3² - r1² 消元，得 x, y 关于 r1 的线性关系：

```
x = D/2 - d12²/(2D) - (d12/D)·r1    →  x = a + b·r1
y = d13²/(2D) - D/2 + (d13/D)·r1    →  y = c + e·r1
```

其中：
```
a = D/2 - d12²/(2D)       b = -d12/D
c = d13²/(2D) - D/2        e = d13/D
```

**第二步：** 代入原点约束 r1² = x² + y²，得二次方程：

```
(b² + e² - 1)·r1² + 2(ab + ce)·r1 + (a² + c²) = 0

即  A·r1² + B·r1 + C = 0
```

**第三步：** 求解并验证：

```
r1 = (-B ± √(B² - 4AC)) / (2A)
```

### 解验证规则

| 条件 | 说明 |
|------|------|
| r1 > 0 | 声源在正半空间 |
| |√(x²+y²) - r1| < 0.5m | 几何一致性校验 |
| |x| < 10D, |y| < 10D | 排除远距离野值 |
| 取最大 r1 | 选远场解 |

### 特点

- **解析解：** 无需迭代，闭式求解
- **计算量小：** 适合嵌入式 MCU 实时处理
- **精度高：** 双曲线精确求交

---

## 7. Theta 三角定位 (远场方法)

### 几何原理

```
线1 (MIC1-MIC2基线):  过中点 (D/2, 0)，方向角 = 90° + θ12
                    单位方向向量 = (-sin θ12, cos θ12)

线2 (MIC1-MIC3基线):  过中点 (0, -D/2)，方向角 = θ13
                    单位方向向量 = (cos θ13, sin θ13)

两条方向线的交点即为声源位置
```

### 求解

设两条线参数方程：
```
p1 + s·dir1 = p2 + t·dir2
```

构造线性方程组 **[dir1, -dir2] · [s, t]^T = p2 - p1**，求解即得交点。

### 与 Fang 算法对比

| 维度 | Fang 算法 | Theta 三角定位 |
|------|-----------|---------------|
| 原理 | TDOA 双曲线精确求交 | 远场到达方向线求交 |
| 精度 | 近场精确，全区域有效 | 当声源距离 >> D 时近似好 |
| 计算 | 二次方程求解 | 线性方程组求解 |
| 角色 | **主算法** | 参考对比验证 |
| 适用场景 | 通用 2D 定位 | 远场快速定位 |

---

## 8. 置信度过滤与质量控制

### 多级质量保证机制

```
┌─────────────────────────────────────────┐
│  第1级: GCC-PHAT 峰值置信度              │
│  confidence < 0.15 → 丢弃当前帧          │
├─────────────────────────────────────────┤
│  第2级: TDOA 物理约束                    │
│  |d12| > D×1.05 或 |d13| > D×1.05 → 丢弃 │
├─────────────────────────────────────────┤
│  第3级: Fang 解合理性校验                 │
│  r1 ≤ 0               → 丢弃             │
│  |r1_est - r1| > 0.5m → 丢弃             │
│  |x| > 10D 或 |y| > 10D → 丢弃            │
├─────────────────────────────────────────┤
│  第4级: 二次插值亚采样点精度              │
│  对互相关峰值做抛物线插值，提高时延精度    │
└─────────────────────────────────────────┘
```

### 阈值参数汇总

| 参数 | 数值 | 用途 |
|------|------|------|
| CONFIDENCE_THRESHOLD | 0.15 | 互相关峰值最低置信度 |
| d12_max / d13_max | D × 1.05 | TDOA 几何延迟容限 |
| r1 误差容限 | 0.5m | Fang 解几何一致性 |
| 位置范围限制 | ±20D | 远场验证(Theta) / ±10D (Fang) |

---

## 9. 可视化输出

### 6 子图布局 (2×3)

```
┌──────────────────────┬──────────────────────┬──────────────────────┐
│  声源位置俯视散点图    │  X 坐标时间序列       │  Y 坐标时间序列       │
│  Fang(红) Theta(蓝)   │                       │                      │
│  MIC三角标记           │                       │                      │
├──────────────────────┼──────────────────────┼──────────────────────┤
│  θ12 角度时序         │  θ13 角度时序         │  互相关置信度         │
│  (MIC1-MIC2基线)      │  (MIC1-MIC3基线)      │  12(橙) 13(蓝)       │
│                      │                       │  阈值线(红虚线)       │
└──────────────────────┴──────────────────────┴──────────────────────┘
```

### 各子图说明

| 子图 | 标题 | X轴 | Y轴 | 图例 |
|------|------|-----|-----|------|
| (0,0) | 声源位置 (俯视图) | X (m) | Y (m) | MIC1=绿三角, MIC2=橙三角, MIC3=蓝三角, Fang=红点, Theta=蓝叉 |
| (0,1) | X 坐标随时间变化 | 帧序号 | X (m) | Fang=红线, Theta=蓝线 |
| (0,2) | Y 坐标随时间变化 | 帧序号 | Y (m) | Fang=红线, Theta=蓝线 |
| (1,0) | 角度 θ12 | 帧序号 | 角度 (°) | 橙色 |
| (1,1) | 角度 θ13 | 帧序号 | 角度 (°) | 蓝色 |
| (1,2) | 互相关置信度 | 帧序号 | 置信度 | 12=橙, 13=蓝, 阈值=红虚线 |

---

## 10. 统计输出

### 每帧日志格式

```
帧   0  lag12= +2.456  lag13= -1.234  d12= +0.052m  d13= -0.026m  Fang(1.23, -0.45)m  Theta(1.20, -0.43)m
```

### 汇总统计

| 指标 | 统计量 |
|------|--------|
| 有效帧率 | 有效帧数 / 总帧数 |
| X 坐标 | 均值、中值、范围 [min, max] |
| Y 坐标 | 均值、中值、范围 [min, max] |
| 距离 | 均值、中值 |

---

## 11. 算法参数总表

| 参数 | 符号 | 数值 | 说明 |
|------|------|------|------|
| 采样率 | Fs | 16000 Hz | 语音信号采样 |
| 声速 | c | 340 m/s | 标准声速 |
| 麦克风间距 | D | 0.17 m | MIC1-MIC2 = MIC1-MIC3 |
| 帧长度 | N | 512 samples | 32ms 帧 |
| FFT 点数 | n_fft | 1024 | GCC-PHAT 的 FFT 点数 |
| 带通范围 | - | 300-3400 Hz | 语音频段 |
| 置信度阈值 | - | 0.15 | 峰值过滤 |
| TDOA 容限 | - | ±1.05D | 物理约束 |

---

## 12. 总结

| 特性 | 实现 |
|------|------|
| **采样** | 16kHz / 512采样点/帧 (32ms) |
| **算法** | GCC-PHAT + Fang 解析解 + Theta 三角定位 |
| **阵列** | 正交 L 型 3 麦克风，基线间距 17cm |
| **预处理** | DC 去除 + 汉宁窗 + 语音带通滤波 (300-3400Hz) |
| **精度增强** | 二次插值亚采样点 + 多级质量过滤 |
| **双方法** | Fang 精确解 (主) + Theta 远场近似 (参考) |
| **计算复杂度** | 解析闭式解，适合实时嵌入式处理 |
| **应用场景** | 2D 实时声源定位 (智能音箱、会议系统、机器人听觉) |
