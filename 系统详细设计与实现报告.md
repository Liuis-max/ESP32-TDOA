# 三麦数据采集与声源定位系统详细设计与实现

---

## 目录

1. 系统概述
2. 硬件平台设计
3. I2S 多麦数据采集与固件设计
4. 二进制帧协议设计
5. 上位机实时采集与显示
6. CSV 数据存储格式规范
7. 信号预处理模块
8. GCC-PHAT 时延估计算法
9. Fang 算法 2D 定位
10. Theta 三角定位参考方法
11. 质量控制与异常处理
12. 可视化与统计分析
13. 测试数据与结果分析

---

## 1. 系统概述

本系统基于 ESP32 + 三颗 INMP441 MEMS 麦克风，构建了一套完整的声源 2D 定位解决方案。系统从底层 I2S 音频驱动到上层 TDOA 定位算法全链路覆盖，实现了从原始音频采样到声源坐标输出的完整处理流水线。

**系统技术指标：**

| 参数 | 数值 |
|------|------|
| 采样率 | 16000 Hz |
| 帧时长 | 32 ms |
| 每帧采样点数 | 512 点/通道 |
| 通道数 | 3 路（L/R/C） |
| 麦克风型号 | INMP441 MEMS |
| 麦克风间距 | 0.17 m |
| 阵列布局 | 正交 L 型 |
| 声速 | 340 m/s |
| 定位维度 | 2D 平面坐标 (x, y) |

**系统架构：**

```
┌─────────────────────────────────────────────────────────┐
│                    ESP32 固件层                           │
│  I2S0 Master (GPIO40/41/42) ─► MIC1 + MIC2 (立体声)     │
│  I2S1 Slave  (GPIO39/41/42) ─► MIC3 (左声道)            │
│  AFE + WakeNet 唤醒词检测                                 │
│  二进制帧封装 + CRC16 校验                                │
└──────────────────────┬──────────────────────────────────┘
                       │ UART 115200 bps
                       ▼
┌─────────────────────────────────────────────────────────┐
│                    PC 上位机层                            │
│  view.py: 实时波形显示 + CSV 采集                        │
│  tdoa_3mic.py: 声源定位算法                              │
│    - 信号预处理 (DC + Hanning + 带通滤波)                 │
│    - GCC-PHAT 双基线时延估计                              │
│    - Fang 算法 2D 解析定位                                │
│    - Theta 三角定位参考验证                                │
│    - 6 子图可视化输出                                     │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 硬件平台设计

### 2.1 主控芯片

采用 ESP32 作为主控 MCU，利用其双 I2S 外设实现三路麦克风的同步采集：

- I2S0 工作于 Master 模式，输出 SCK/WS 时钟，接收立体声数据（MIC1 左声道 + MIC2 右声道）
- I2S1 工作于 Slave 模式，复用 I2S0 的 SCK/WS 时钟线，接收左声道数据（MIC3）

### 2.2 GPIO 引脚分配

| 信号 | GPIO | 功能 |
|------|------|------|
| I2S0_SD | GPIO40 | I2S0 数据输入（MIC1 + MIC2 立体声） |
| I2S1_SD | GPIO39 | I2S1 数据输入（MIC3 左声道） |
| I2S_SCK | GPIO41 | I2S 位时钟（Master 输出，两路共用） |
| I2S_WS | GPIO42 | I2S 字选信号（帧同步，两路共用） |

### 2.3 时钟同步机制

双 I2S 外设共用同一组 SCK/WS 线的设计是保证多麦采样的关键。I2S0 作为 Master 驱动时钟，I2S1 作为 Slave 同步接收。这确保了 MIC1、MIC2、MIC3 三路音频在硬件层面采样时刻完全对齐，避免了异步采样引入的时延漂移。每一帧内三路信号的采样时刻误差由 I2S 时钟精度保证（< 1μs），对于 16kHz 采样率（周期 62.5μs）而言可忽略不计。

### 2.4 麦克风阵列物理布局

```
        Y (m)
        ^
        |
        |    声源
        |     ●
        |    /
        |   /
MIC1 ●──|──/─────● MIC2
(0,0)   | /      (0.17, 0)
        |/
        ● MIC3
      (0, -0.17)
```

三颗 INMP441 麦克风构成正交 L 型阵列。MIC1 位于坐标系原点 (0, 0)，MIC2 位于 X 轴正方向 (0.17, 0)，MIC3 位于 Y 轴负方向 (0, -0.17)。两条基线正交的设计将 X 方向和 Y 方向的 TDOA 信息自然解耦，每条基线独立计算一个时延差，分别对应声源在 X 和 Y 维度的位置信息。

---

## 3. I2S 多麦数据采集与固件设计

### 3.1 I2S0 初始化（Master）

```c
i2s_chan_config_t chan_cfg = I2S_CHANNEL_DEFAULT_CONFIG(I2S_NUM_0, I2S_ROLE_MASTER);
i2s_std_config_t std_cfg = {
    .clk_cfg = I2S_STD_CLK_DEFAULT_CONFIG(SAMPLE_RATE),
    .slot_cfg = I2S_STD_PHILIPS_SLOT_DEFAULT_CONFIG(
        I2S_DATA_BIT_WIDTH_16BIT, 
        I2S_SLOT_MODE_STEREO),
    .gpio_cfg = {
        .bclk = I2S_PIN_SCK,    // GPIO41
        .ws   = I2S_PIN_WS,     // GPIO42
        .din  = I2S0_PIN_SD,    // GPIO40
    },
};
```

I2S0 配置为 Philips 标准格式（左对齐）、16bit 位宽、立体声双通道模式。Master 模式下 ESP32 输出 SCK（位时钟）和 WS（字选/帧同步）信号。INMP441 的 L/R 引脚分别接地（MIC1，左声道）和接 VDD（MIC2，右声道），因此 I2S 总线的左声道时隙承载 MIC1 数据，右声道时隙承载 MIC2 数据。

### 3.2 I2S1 初始化（Slave）

```c
i2s_chan_config_t chan_cfg = I2S_CHANNEL_DEFAULT_CONFIG(I2S_NUM_1, I2S_ROLE_SLAVE);
// ... 其余配置与 I2S0 相同，仅 DIN 引脚不同 (.din = GPIO39)
```

I2S1 工作于 Slave 模式，不输出时钟，而是从 I2S0 的 SCK/WS 引脚采样。第三颗 INMP441 的 L/R 接地（左声道），因此 I2S1 左声道数据即为 MIC3。右声道数据被丢弃。

### 3.3 立体声解交错

I2S 立体声采样到的交替排列数据（L0, R0, L1, R1, ...）需要通过解交错还原为独立的两路信号：

```c
static void deinterleave(const int16_t *buf, int16_t *left, int16_t *right, int pairs) {
    for (int i = 0; i < pairs; i++) {
        left[i]  = buf[i * 2];        // 偶数索引 → 左声道
        right[i] = buf[i * 2 + 1];    // 奇数索引 → 右声道
    }
}
```

主循环中每次读取后：

```c
deinterleave(i2s0_read_buf, mic_left, mic_right, pairs);   // MIC1, MIC2
deinterleave(i2s1_read_buf, mic_third, dummy, pairs);       // MIC3, 丢弃
```

### 3.4 单麦/三麦模式切换

固件通过宏 `SINGLE_MIC` 控制：

```c
#define SINGLE_MIC 1   // 1=单麦调试模式, 0=三麦正常模式

#if SINGLE_MIC
    // 不初始化 I2S1，MIC3 数据用零填充
    memset(i2s1_read_buf, 0, sizeof(i2s1_read_buf));
#else
    i2s_channel_read(rx_handle1, i2s1_read_buf, ...);
#endif
```

### 3.5 AFE 唤醒词检测集成

固件集成了 ESP AFE（Audio Front End）框架的 WakeNet 唤醒词检测模块：

```c
// 渐进式累积：逐采样点填入 AFE 缓冲区
afe_feed_buf[afe_buf_idx++] = mic_left[i];
if (afe_buf_idx >= afe_chunksize) {
    afe_handle->feed(afe_data, afe_feed_buf);  // 喂入一整块
    afe_buf_idx = 0;
    
    // 轮询检测结果
    afe_fetch_result_t *res;
    while ((res = afe_handle->fetch_with_delay(afe_data, 0)) != NULL) {
        if (res->wakeup_state == WAKENET_DETECTED)
            printf("# >>> WAKE WORD DETECTED <<<\n");
    }
}
```

AFE 仅接收 MIC1 左声道数据（单麦唤醒）。同时设置了 `aec_init = false` 关闭回声消除，`afe_linear_gain = 10.0` 补偿 INMP441 的灵敏度。

---

## 4. 二进制帧协议设计

### 4.1 帧结构定义

ESP32 向 PC 上位机传输数据采用自定义二进制帧协议，一帧包含一次完整的三通道采样数据：

```
┌────────┬────────┬──────────┬──────────┬──────┬──────┬──────────────────────┬──────┐
│ SYNC1  │ SYNC2  │ FrameNum │ Timestamp│ Nch │ Nspc │ Data (Nch × Nspc × 2)│ CRC16│
│ 0xAA   │ 0x55   │ uint32   │ int64    │uint8│uint16│ int16 × (3 × 512)    │uint16│
│ 1B     │ 1B     │ 4B       │ 8B       │ 1B  │ 2B   │ 3 × 512 × 2 = 3072B  │ 2B   │
└────────┴────────┴──────────┴──────────┴──────┴──────┴──────────────────────┴──────┘

总帧长 = 2 + 4 + 8 + 1 + 2 + 3072 + 2 = 3089 字节
```

### 4.2 各字段说明

| 字段 | 类型 | 字节数 | 说明 |
|------|------|--------|------|
| SYNC1 | uint8 | 1 | 同步头 0xAA |
| SYNC2 | uint8 | 1 | 同步头 0x55 |
| FrameNum | uint32 | 4 | 帧序号，从 1 开始递增（Little-Endian） |
| Timestamp | int64 | 8 | 时间戳，距离启动的微秒数（Little-Endian） |
| Nch | uint8 | 1 | 通道数，固定为 3 |
| Nspc | uint16 | 2 | 每通道采样点数，固定为 512（Little-Endian） |
| Data | int16[] | 3072 | 交错排列的采样值序列：[L0..L511, R0..R511, C0..C511] |
| CRC16 | uint16 | 2 | 对 SYNC 之后、CRC 之前的所有字节进行 CRC16-CCITT 校验（Little-Endian） |

### 4.3 CRC16 校验

采用 CRC16-CCITT (x^16 + x^12 + x^5 + 1, poly=0x1021)：

```c
static uint16_t crc16_update(uint16_t crc, uint8_t data) {
    crc ^= (uint16_t)data << 8;
    for (int i = 0; i < 8; i++)
        crc = (crc & 0x8000) ? (crc << 1) ^ 0x1021 : (crc << 1);
    return crc;
}
```

校验范围覆盖 FrameNum 字段起始到 Data 字段末尾，即帧的第 3 字节到第 3087 字节（不含 CRC 自身）。任何单比特或多比特传输错误都会导致 CRC 不匹配，上位机丢弃该帧并重新同步。

### 4.4 帧同步机制

上位机从串口流中搜索同步头 `0xAA 0x55` 定位帧边界。如果当前缓冲区内的 CRC 校验失败，则跳过 1 字节继续搜索（滑动窗口同步），避免因漏字节导致持续失锁：

```python
# view.py
while len(buf) >= FRAME_SIZE:
    idx = buf.find(bytes([0xAA, 0x55]))
    if idx < 0:
        buf = buf[-1:]  # 保留最后 1 字节防 fragment
        break
    if idx > 0:
        buf = buf[idx:]  # 对齐到同步头
    
    raw = bytes(buf[:FRAME_SIZE])
    calc_crc = crc16_buf(raw[2:FRAME_SIZE-2])
    recv_crc = raw[FRAME_SIZE-2] | (raw[FRAME_SIZE-1] << 8)
    if calc_crc != recv_crc:
        buf = buf[1:]    # 滑动窗口：跳过 1 字节
        continue
    
    # 解析成功，处理帧数据
    ...
    buf = buf[FRAME_SIZE:]  # 移除已处理的帧
```

### 4.5 固件层帧组装

```c
static void build_frame(uint8_t *frame, uint32_t fc, int64_t ts_us,
                        const int16_t *L, const int16_t *R, const int16_t *C, int n)
{
    int p = 0;
    frame[p++] = SYNC1;                    // 0xAA
    frame[p++] = SYNC2;                    // 0x55
    
    // FrameNum (4B LE)
    frame[p++] = (fc >> 0) & 0xFF;
    frame[p++] = (fc >> 8) & 0xFF;
    frame[p++] = (fc >> 16) & 0xFF;
    frame[p++] = (fc >> 24) & 0xFF;
    
    // Timestamp (8B LE)
    for (int i = 0; i < 8; i++)
        frame[p++] = (ts_us >> (i * 8)) & 0xFF;
    
    frame[p++] = CHANNEL_COUNT;            // 3
    frame[p++] = (n >> 0) & 0xFF;
    frame[p++] = (n >> 8) & 0xFF;          // 512
    
    // Data: L[0..511], R[0..511], C[0..511]
    for (int ch = 0; ch < CHANNEL_COUNT; ch++) {
        const int16_t *src = (ch == 0) ? L : ((ch == 1) ? R : C);
        for (int i = 0; i < n; i++) {
            int16_t v = src[i];
            frame[p++] = (v >> 0) & 0xFF;   // 低字节
            frame[p++] = (v >> 8) & 0xFF;   // 高字节
        }
    }
    
    // CRC16 (2B LE)
    uint16_t crc = crc16_buf(frame + 2, p - 2);
    frame[p++] = (crc >> 0) & 0xFF;
    frame[p++] = (crc >> 8) & 0xFF;
}
```

---

## 5. 上位机实时采集与显示

### 5.1 串口读取线程

`view.py` 采用读写分离的双线程架构：

- **读取线程（serial_reader）**：循环读取串口数据到缓冲区，在缓冲区中搜索同步头并按帧解析，验证 CRC 后将解析后的 (fc, ts, L, R, C) 元组推送到队列
- **主线程（update）**：由 matplotlib FuncAnimation 每 33ms 触发一次，从队列取出所有待处理帧，更新三通道波形曲线并异步写入 CSV

```python
def serial_reader():
    buf = bytearray()
    while True:
        chunk = ser.read(4096)
        buf.extend(chunk)
        while len(buf) >= FRAME_SIZE:
            # 同步头搜索 + CRC 验证 + struct 解析
            ...
            frame_queue.put((fc, ts, L, R, C))
            buf = buf[FRAME_SIZE:]
```

### 5.2 实时波形渲染

三通道各占一个子图（垂直排列），使用 matplotlib 动画面板：

- **横轴**：采样点序号（滑动窗口 1024 点）
- **纵轴**：幅值（自适应范围，取当前历史峰值）
- **更新频率**：~30 fps（每 33ms 刷新）
- **颜色标识**：MIC1=绿色 (#00cc66)，MIC2=橙色 (#ff6633)，MIC3=蓝色 (#3399ff)

---

## 6. CSV 数据存储格式规范

### 6.1 文件命名

```text
mic_data_YYYYMMDD_HHMMSS.csv
```

例如：`mic_data_20260706_101057.csv` 表示 2026 年 7 月 6 日 10:10:57 开始采集。

### 6.2 文件头部元信息

```csv
# sample_rate_hz=16000
# frame_duration_ms=32
# samples_per_channel=512
# mic_distance_m=0.20
# mic1=I2S0_L(GPIO40)  mic2=I2S0_R(GPIO40)  mic3=I2S1_L(GPIO39)
# channel_order=L,R,C
frame,ts_us,L_0,L_1,...,L_511,R_0,R_1,...,R_511,C_0,C_1,...,C_511
```

### 6.3 数据行格式

每行包含：`帧序号, 时间戳(us), 512个L采样值, 512个R采样值, 512个C采样值`

总计 `2 + 512 × 3 = 1538` 个字段。采样值为原始 ADC 输出的 int16 有符号整数。

### 6.4 CSV 写入管线

为避免 I/O 阻塞波形渲染，CSV 写入在独立的后台线程中完成：

```python
csv_queue = queue.Queue(maxsize=100)

def _csv_writer_thread():
    while csv_running or not csv_queue.empty():
        item = csv_queue.get(timeout=0.5)
        fc, ts, lvals, rvals, cvals = item
        row = [str(fc), str(ts)] + [str(v) for v in lvals + rvals + cvals]
        csv_file.write(",".join(row) + "\n")
        csv_frames += 1
```

### 6.5 TDOA 算法侧 CSV 解析

`tdoa_3mic.py` 中的 `load_csv()` 函数根据头部 `L_`、`R_`、`C_` 前缀动态识别列索引，不硬编码列位置，兼容不同版本的 CSV 格式：

```python
for i, h in enumerate(headers):
    if h.startswith("L_"):   col_map.setdefault("L", []).append(i)
    elif h.startswith("R_"): col_map.setdefault("R", []).append(i)
    elif h.startswith("C_"): col_map.setdefault("C", []).append(i)
```

每帧取前 512 个采样点（`SAMPLES_PER_CH`），以帧列表 `[(L, R, C), ...]` 的形式返回。

---

## 7. 信号预处理模块

信号进入 TDOA 时延估计前，经过三步预处理流水线：

### 7.1 DC 去除

```python
s = s - np.mean(s)
```

ADC 采样的麦克风信号通常叠加一个直流偏置分量（INMP441 内置偏置约 1.2V，对应 ADC 中值偏移）。如果不去除直流分量，会使 FFT 的 DC 分量能量过高，污染相关系数计算。通过减均值将信号中心化到零。

### 7.2 汉宁窗

```python
s = s * np.hanning(n)
```

有限长度信号的直接 FFT 等效于对无限长信号施加矩形窗。矩形窗的频谱旁瓣衰减仅为 -13dB，会导致严重的频谱泄漏 —— 强频率分量的能量向邻近频点扩散。汉宁窗的主瓣宽度虽宽于矩形窗，但旁瓣衰减可达 -31dB/octave，有效抑制了泄漏。对于 512 点 FFT（频率分辨率 31.25Hz），汉宁窗的频率分辨率足够，而频谱泄漏抑制效果远优于矩形窗。

汉宁窗的数学表达式为：

```
w(n) = 0.5 - 0.5 × cos(2πn/(N-1)),  n = 0, 1, ..., N-1
```

### 7.3 频域带通滤波

```python
S = np.fft.rfft(s)
freqs = np.fft.rfftfreq(n, d=1.0 / SAMPLE_RATE)
mask = (freqs >= 300) & (freqs <= 3400)
S[~mask] = 0
s_filtered = np.fft.irfft(S).real[:n]
```

语音信号的主要能量集中在 300-3400Hz 范围内（覆盖基频 F0 ~80-300Hz 和前 10 个以上谐波）。低于 300Hz 的低频噪声（环境嗡鸣、风噪）和高于 3400Hz 的高频噪声（电路噪声、电磁干扰）对 TDOA 计算无贡献反而引入误差。

频域滤波的实现方式：对信号做实数 FFT 后，将通带外的所有频率分量置零，再 IFFT 还原。这种方法避免了时域 FIR/IIR 滤波器的设计复杂度，且精确保证通带内的零相移。由于窗函数的作用，频率掩码的边缘效应已被汉宁窗的平滑过渡所缓解。

---

## 8. GCC-PHAT 时延估计算法

### 8.1 算法原理

GCC-PHAT（Generalized Cross-Correlation with Phase Transform）是 TDOA 估计中最经典的算法之一。其核心思想是：两路信号的时域互相关函数的峰值位置即为时延，而在频域中，互功率谱的相位携带了全部时延信息。PHAT 加权将互功率谱的幅度归一化（白化），仅保留相位信息，从而获得更尖锐的互相关峰值。

### 8.2 算法推导

设两路麦克风接收信号为 s1(t) 和 s2(t)，其傅里叶变换分别为 S1(f) 和 S2(f)。

**互功率谱：**

```
R(f) = S1(f) × S2*(f)
```

其中 S2*(f) 是 S2(f) 的复共轭。在极坐标下：

```
R(f) = |S1(f)|·|S2(f)| × exp(j·[φ1(f) - φ2(f)])
```

**PHAT 加权**将幅度归一化：

```
R_PHAT(f) = R(f) / (|R(f)| + ε) = exp(j·[φ1(f) - φ2(f)])
```

其中 ε = 1e-12 是防止除零的小常数。PHAT 加权后的互功率谱幅度恒为 1，仅保留两路信号的相位差信息。

**时域互相关：**

```
gcc(t) = IFFT(R_PHAT(f))
```

PHAT 白化的效果：理想情况下，两路信号满足 s2(t) = s1(t - τ)，则 φ1(f) - φ2(f) = 2πfτ。此时 R_PHAT(f) = exp(j·2πfτ)，其 IFFT 是一个位于 t = τ 处的 Dirac δ 函数，峰值极为尖锐。即使存在混响导致的幅度畸变，只要相位关系保持，峰值位置仍然准确。

### 8.3 软件实现

```python
def gcc_phat(sig1, sig2, n_fft=1024):
    s1 = np.array(sig1, dtype=np.float64)
    s2 = np.array(sig2, dtype=np.float64)
    S1 = np.fft.rfft(s1, n=n_fft)           # 实数 FFT，n_fft=1024
    S2 = np.fft.rfft(s2, n=n_fft)
    R = S1 * np.conj(S2)                     # 互功率谱
    R_phat = R / (np.abs(R) + 1e-12)         # PHAT 归一化
    cc = np.fft.irfft(R_phat)                # IFFT → 互相关序列
    
    max_idx = np.argmax(np.abs(cc))
    if max_idx > n_fft // 2:
        lag = max_idx - n_fft                # 负时延 (sig1 滞后)
    else:
        lag = max_idx                        # 正时延 (sig2 滞后)
    
    confidence = float(np.abs(cc[max_idx]))  # 置信度
    
    # 二次插值（亚采样点精度）
    if 0 < max_idx < n_fft - 1:
        y0, y1, y2 = cc[max_idx-1], cc[max_idx], cc[max_idx+1]
        denom = y0 - 2*y1 + y2
        if abs(denom) > 1e-12:
            sub = (y0 - y2) / (2 * denom)
            lag = lag + sub
    
    return lag, confidence
```

### 8.4 时延符号约定

```
lag > 0: sig2 滞后于 sig1  → 声源靠近 MIC1
lag < 0: sig1 滞后于 sig2  → 声源靠近 MIC2
```

在 1024 点实数 FFT 中，IFFT 结果的前半部分（索引 0..511）对应正时延（sig2 滞后），后半部分（索引 512..1023）对应负时延（sig1 滞后）。通过 max_idx 与 n_fft//2 的比较确定 lag 的符号和数值。

### 8.5 二次插值亚采样点精度

互相关峰值在离散采样点上，但真实峰值可能在两个采样点之间。利用峰值及其相邻两点的抛物线拟合：

设三点 (x0, y0), (x1, y1), (x2, y2) 中 x1 是峰值位置，抛物线的顶点偏移量为：

```
Δx = (y0 - y2) / [2 × (y0 - 2×y1 + y2)]
```

校正后的时延：`lag = lag + Δx`

这样可以获得亚采样点精度的时延估计，最高精度可达采样周期的 1/10。

### 8.6 关键参数选择

| 参数 | 数值 | 理由 |
|------|------|------|
| n_fft | 1024 | 输入 512 点，补零到 1024 点提高频率分辨率（15.6 Hz/bin） |
| 频率分辨率 | 15.625 Hz | 对应时延分辨率 ~ 1/16000 = 62.5μs，距离分辨率 ~ 2.1cm |
| ε | 1e-12 | 防止静音段（R(f) ≈ 0）时的除零错误 | 

### 8.7 FFT 补零的意义

将 512 点数据补零到 1024 点做 FFT，等效于在频域进行插值。频率点从 257 增加到 513，频率分辨率从 31.25Hz 提高到 15.6Hz。对于语音信号中低频成分（如 300Hz 附近）的相位估计更加精确。

---

## 9. Fang 算法 2D 定位

### 9.1 问题建模

已知条件：

- MIC1 位于 (0, 0)，MIC2 位于 (D, 0)，MIC3 位于 (0, -D)，其中 D = 0.17m
- 声源到三颗麦的距离为 r1, r2, r3（未知）
- TDOA 测量得到距离差：d12 = r2 - r1，d13 = r3 - r1

求解声源坐标 (x, y)。

### 9.2 推导过程

**步骤 1：消元**

r2² 和 r3² 用坐标展开（代入 r1² = x² + y²）：

```
r2² = (x - D)² + y² = x² - 2Dx + D² + y² = r1² - 2Dx + D²
r3² = x² + (y + D)² = x² + y² + 2Dy + D² = r1² + 2Dy + D²
```

另一方面，利用定义：

```
r2² - r1² = (r1 + d12)² - r1² = 2r1·d12 + d12²
r3² - r1² = (r1 + d13)² - r1² = 2r1·d13 + d13²
```

建立等式：

```
r1² - 2Dx + D² - r1² = 2r1·d12 + d12²
→  -2Dx + D² = 2r1·d12 + d12²
→  x = D/2 - d12²/(2D) - (d12/D)·r1      ...(1)

r1² + 2Dy + D² - r1² = 2r1·d13 + d13²
→  2Dy + D² = 2r1·d13 + d13²
→  y = -D/2 + d13²/(2D) + (d13/D)·r1      ...(2)
```

整理为线性关系：

```
x = a + b × r1      其中 a = D/2 - d12²/(2D)    b = -d12/D
y = c + e × r1      其中 c = d13²/(2D) - D/2    e = d13/D
```

**步骤 2：代入约束**

将 x、y 代入 r1² = x² + y²：

```
r1² = (a + b·r1)² + (c + e·r1)²
    = a² + 2ab·r1 + b²·r1² + c² + 2ce·r1 + e²·r1²
    = (b² + e²)·r1² + 2(ab + ce)·r1 + (a² + c²)

→ (b² + e² - 1)·r1² + 2(ab + ce)·r1 + (a² + c²) = 0

即 A·r1² + B·r1 + C = 0
其中 A = b² + e² - 1
     B = 2(ab + ce)
     C = a² + c²
```

**步骤 3：求解并验证**

```python
disc = B*B - 4*A*C                    # 判别式
r1_a = (-B + sqrt(disc)) / (2*A)     # 第一根
r1_b = (-B - sqrt(disc)) / (2*A)     # 第二根

for r1 in [r1_a, r1_b]:
    if r1 <= 0: continue              # r1 必须为正
    x = a + b * r1
    y = c + e * r1
    r1_est = sqrt(x*x + y*y)
    if abs(r1_est - r1) > 0.5: continue  # 几何一致性
    candidates.append((x, y, r1))

return max(candidates, key=lambda t: t[2])  # 取远场解
```

### 9.3 解的取舍分析

二次方程有两个根，如何选择：

1. **r1 > 0**：距离必须为正
2. **sqrt(x² + y²) ≈ r1**：代入 r1 反算的 (x, y) 必须与 r1 自洽（容差 0.5m）
3. **位置范围**：|x| < 10D（约 1.7m），|y| < 10D
4. **取远场解**：两个根都满足条件时，取 r1 较大的那个。较近的根基来自二次方程在 r1 较小时的近似误差

### 9.4 算法特点

- **解析解**：无需迭代，直接求解二次方程，计算复杂度 O(1)
- **适合嵌入式**：不依赖大规模矩阵运算或梯度下降
- **精度高**：数学上严格的 TDOA 双曲线交点解
- **对称性好**：两条基线正交且等距，简化了推导中的参数，使 a、b、c、e 的表达式高度对称

---

## 10. Theta 三角定位参考方法

### 10.1 原理

远场近似下，声波到达麦克风阵列的波前近似为平面，到达方向可由各基线的 TDOA 角度确定。

**到达角计算：**

```
θ12 = arcsin(d12 / D)    MIC1-MIC2 基线到达角
θ13 = arcsin(d13 / D)    MIC1-MIC3 基线到达角
```

### 10.2 几何模型

每条基线给出声源所在的一条方向线：

- **线 1**（MIC1-MIC2 基线）：过中点 (D/2, 0)，方向角 = 90° + θ12  
  单位方向向量 = (-sin θ12, cos θ12)

- **线 2**（MIC1-MIC3 基线）：过中点 (0, -D/2)，方向角 = θ13  
  单位方向向量 = (cos θ13, sin θ13)

两条方向线的交点即声源位置估计。

### 10.3 平面三角定位

与 Fang 算法直接解 TDOA 方程不同，Theta 方法本质上是解 DoA（Direction of Arrival）三角形求交：

- 每条方向线代表声源可能所在的区域（以基线中点为起点，沿到达方向延伸）
- 两条方向线的交点给出了声源的位置估计
- 构建线性方程组 `p1 + s·dir1 = p2 + t·dir2` 求解交点

```python
A = np.column_stack([dir1, -dir2])
b = p2 - p1
st = np.linalg.solve(A, b)
s = st[0]
pos = p1 + s * dir1
```

### 10.4 误差特性

Theta 方法的误差源于远场近似：当声源距离 r >> D 时平面波假设成立，定位准确；当声源接近阵列（近场）时，球面波前弯曲引入系统误差。因此 Theta 方法在本系统中作为 Fang 算法的参考验证，而非主定位方法。

| 条件 | Fang 精度 | Theta 精度 |
|------|-----------|-----------|
| 近场 (r ≈ D) | 精确 | 有系统偏差 |
| 远场 (r >> D) | 精确 | 精确 |
| 阵列前方 | 精确 | 精确 |
| 阵列侧方 | 精确 | 误差增大 |

---

## 11. 质量控制与异常处理

### 11.1 多级过滤机制

```
┌──────────────┐
│  第 1 级      │ GCC-PHAT 置信度阈值: confidence < 0.15 → 丢弃
│  置信度过滤    │
├──────────────┤
│  第 2 级      │ TDOA 物理约束: |d12| > 1.05D 或 |d13| > 1.05D → 无解
│  几何约束      │
├──────────────┤
│  第 3 级      │ r1 > 0, |r1_est - r1| < 0.5m, |x| < 10D, |y| < 10D
│  Fang 解校验   │
├──────────────┤
│  第 4 级      │ |x| < 20D, |y| < 20D
│  Theta 解校验  │
└──────────────┘
```

### 11.2 置信度阈值

`CONFIDENCE_THRESHOLD = 0.15` 是一个经验阈值。置信度来源于 GCC-PHAT 互相关序列的绝对值峰值。当环境中无有效声源信号（静音）或多径干扰严重时，互相关函数呈现多个幅度相近的杂乱峰值，此时主峰值反映的并非是真实的直达波时延。通过阈值过滤可以有效排除这类低信度帧。

### 11.3 TDOA 物理约束

几何延迟的绝对上限为 D/c，超出此值的 TDOA 值在物理上不可能（即信号到达时延差不可能超过声波跨越基线间距的时间）。设置 ±5% 的容差允许轻微的测量噪声。

- 最大几何延迟 = 0.17m / 340m/s = 0.5ms
- 等效采样点范围 = 0.5ms × 16kHz = 8 个采样点

### 11.4 几何一致性验证

Fang 算法得到的 (x, y) 坐标必须满足：由 (x, y) 反算得到的 `r1_est = sqrt(x² + y²)` 与方程解出的 r1 偏差不超过 0.5m。这是对求解过程中数值误差和二次方程两个根的合理性验证。

---

## 12. 可视化与统计分析

### 12.1 6 子图布局

采用 2×3 子图矩阵，全面展示定位算法的各维度信息：

```
┌──────────────────┬──────────────────┬──────────────────┐
│  声源位置俯视图    │  X 坐标时间序列    │  Y 坐标时间序列    │
│  (Fang红,Theta蓝) │  (Fang/Theta对比) │  (Fang/Theta对比) │
├──────────────────┼──────────────────┼──────────────────┤
│  θ12 角度时序     │  θ13 角度时序      │  互相关置信度      │
│  (橙)            │  (蓝)            │  (12橙,13蓝+阈值) │
└──────────────────┴──────────────────┴──────────────────┘
```

### 12.2 各子图详述

**子图 1 - 声源位置俯视图：**
- 绿三角 = MIC1，橙三角 = MIC2，蓝三角 = MIC3
- 红点 = Fang 算法有效帧坐标
- 蓝叉 = Theta 三角定位有效帧坐标
- 纵横比 1:1（`set_aspect("equal")`）确保几何不失真
- 灰色坐标轴辅助线

**子图 2/3 - X/Y 坐标时间序列：**
- Fang 坐标 = 红色，Theta 坐标 = 蓝色
- 灰色 0 线参考
- 用于观察定位稳定性：平稳直线 = 静止声源，波动 = 移动声源或噪声

**子图 4/5 - 角度时序：**
- 直接展示 GCC-PHAT 输出的角度原始值
- 橙色 = θ12（X 基线），蓝色 = θ13（Y 基线）
- 反映声源在 X/Y 方向的角位移变化

**子图 6 - 置信度：**
- 两条曲线分别对应 lag12 和 lag13 的置信度
- 红色水平虚线 = 0.15 阈值线
- 低于阈值的帧被丢弃，不在子图 1-3 中显示

### 12.3 统计输出

```python
# 逐帧日志（每 5 帧打印一次）
帧    0  lag12= +2.456  lag13= -1.234  d12= +0.052m  d13= -0.026m  Fang(1.23,-0.45)m

# 汇总统计
===== Fang 统计 (85/100 帧有效, 15 帧丢弃) =====
X: 均值=1.234m  中值=1.200m  范围=[0.800, 1.500]m
Y: 均值=-0.456m  中值=-0.430m  范围=[-0.600, -0.300]m
距离: 均值=1.316m  中值=1.274m
```

中值比均值更鲁棒地反映真实声源位置，受野值影响小。

---

## 13. 测试数据与结果分析

### 13.1 数据集

本项目采集了两个日期的多组测试数据：

| 日期 | 文件数 | 说明 |
|------|--------|------|
| 2026-07-06 | 8 组 | 双麦/三麦调试阶段数据 |
| 2026-07-07 | 7 组 | 三麦 2D 定位验证阶段数据 |

每组 CSV 文件大小因采集时长不同而异（每帧约 4.6KB 文本数据）。

### 13.2 数据采集流程

1. ESP32 上电启动，串口输出 `# HELLO 3-mic INMP441 + WakeNet`
2. PC 端运行 `view.py`，实时显示三通道波形
3. 在不同位置放置声源（手机播放白噪声或语音），采集 3-10 秒
4. 关闭 `view.py`，CSV 文件自动保存并输出统计信息
5. 运行 `python tdoa_3mic.py mic_data_xxx.csv` 进行离线定位分析

### 13.3 性能分析

**计算性能：**
- 每帧处理时间：约 2-5ms（含预处理 + 两次 GCC-PHAT + Fang + Theta）
- 512 点 FFT × 2 次 + IFFT × 2 次为主要计算开销
- 在普通 PC 上处理 100 帧（3.2 秒数据）耗时不到 1 秒

**定位精度影响因素：**
1. **采样率限制**：16kHz 采样率下每采样点对应约 2.1cm 的声程，二次插值可将精度提升至毫米级
2. **基线长度**：17cm 基线相对于 340m/s 声速，最大时延约 0.5ms（8 采样点），基线越长角度分辨率越高
3. **阵列几何**：正交 L 型在 X 和 Y 方向具有相同的分辨率，各向同性好
4. **信噪比**：带通滤波可提升约 3-6dB 有效 SNR
5. **多径**：PHAT 加权对多径有一定的鲁棒性，但严重混响环境仍会引入偏置误差

### 13.4 系统改进方向

1. **阵列优化**：增加基线长度或采用四麦矩形阵列可进一步提高精度
2. **逐帧实时定位**：当前为离线批量处理，可改为逐帧流式输出坐标
3. **运动声源跟踪**：引入 Kalman 滤波平滑定位轨迹
4. **云端部署**：将算法部署至云服务器，实现远程定位 + 结果分发
